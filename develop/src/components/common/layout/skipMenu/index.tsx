import * as A from './styles';

export default function SkipMenu(): JSX.Element {
    return (
        <A.SkipBox>
            <A.SkipLink href="#main">본문 바로가기</A.SkipLink>
        </A.SkipBox>
    );
}
